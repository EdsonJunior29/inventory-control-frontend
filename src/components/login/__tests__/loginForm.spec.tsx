import { render } from '@testing-library/react';
import { LoginForm } from '../loginForm';

describe('Render Login Form component', () => {
  it('Login Form Componente', () => {
    render(<LoginForm />);
  });
});
